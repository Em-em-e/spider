<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fishroad.dao.IUserDao">

	<select id="queryUser" parameterType="com.fishroad.vo.User"
		resultType="com.fishroad.vo.User">
		select t.id as id,
		t.user_name as username,
		t.age as age
		from
		user_t t
	</select>


</mapper>
